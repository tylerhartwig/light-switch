<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="{Binding Title}" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:LightSwitch;Assembly=LightSwitch" x:Class="LightSwitch.AddEditLightBulbPage">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save" Command="{Binding SaveLightBulb}" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<TableView Intent="Form" HasUnevenRows="true">
			<TableView.Behaviors>
				<local:DynamicListBehavior ItemsSource="{Binding Contacts}" 
										   TableSectionName="ContactsSection">
					<local:DynamicListBehavior.ItemTemplate>
						<DataTemplate>
							<TextCell Text="{Binding Name}" />
						</DataTemplate>
					</local:DynamicListBehavior.ItemTemplate>
				</local:DynamicListBehavior>
			</TableView.Behaviors>
			<TableView.Root>
				<TableSection Title="Light Bulb Name">
					<ViewCell>
						<Entry Text="{Binding Name}" HeightRequest="40"/>
					</ViewCell>
				</TableSection>
				<TableSection Title="Message">
					<ViewCell>
						<Editor Text="{Binding Message}" HeightRequest="120" />
					</ViewCell>
				</TableSection>
				<TableSection Title="Contacts" x:Name="ContactsSection">
					<ViewCell>
						<Button Text="Add Contact" Command="{Binding AddContacts}" />
					</ViewCell>
				</TableSection>
			</TableView.Root>
		</TableView>
	</ContentPage.Content>
</ContentPage>
