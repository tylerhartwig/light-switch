<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Light Bulbs" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:LightSwitch;Assembly=LightSwitch" x:Class="LightSwitch.MainPage">
	<ContentPage.Behaviors>
		<local:EventToCommandBehavior EventName="Appearing" Command="{Binding RefreshLightBulbs}" />
	</ContentPage.Behaviors>
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="+" Command="{Binding AddLightBulb}" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<ListView ItemsSource="{Binding LightBulbs}">
			<ListView.ItemTemplate>
				<DataTemplate>
					<TextCell Text="{Binding Name}" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>
